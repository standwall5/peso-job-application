# Architecture Overview: VerifiedIdUpload Component

## 🏗️ Component Architecture

```
┌─────────────────────────────────────────────────────────────────┐
│                     VerifiedIdUpload.tsx                        │
│                    (Main Orchestrator)                          │
│                                                                 │
│  Responsibilities:                                              │
│  - Route between view/edit modes                               │
│  - Handle form submission                                       │
│  - Manage global state (editMode, message, etc.)              │
│  - Coordinate between hooks and components                     │
└────────────┬──────────────────────────────────┬─────────────────┘
             │                                  │
             ├──────────────────────────────────┘
             │
    ┌────────┴─────────┐
    │                  │
    ▼                  ▼
┌────────────┐    ┌────────────┐
│  Custom    │    │  Custom    │
│   Hooks    │    │ Components │
└────────────┘    └────────────┘
```

## 📦 Module Dependency Graph

```
VerifiedIdUpload.tsx
├── hooks/
│   ├── useExistingId.ts
│   │   └── @/lib/db/services/applicant-id.service
│   │
│   └── useIdForm.ts
│       ├── types.ts
│       └── utils.ts (createFilePreview)
│
├── components/
│   ├── IdViewMode.tsx
│   │   ├── @/components/Button
│   │   ├── utils.ts (getImageUrl, formatUploadDate)
│   │   ├── types.ts
│   │   ├── MessageBanner.tsx
│   │   ├── ImageViewCard.tsx
│   │   │   ├── constants.ts
│   │   │   └── styles
│   │   └── ImageModal.tsx
│   │       ├── types.ts
│   │       └── styles
│   │
│   └── IdEditMode.tsx
│       ├── @/components/Button
│       ├── types.ts
│       ├── utils.ts (getImageUrl)
│       ├── MessageBanner.tsx
│       ├── StepIndicator.tsx
│       │   └── styles
│       ├── IdTypeSelector.tsx
│       │   ├── constants.ts
│       │   └── styles
│       ├── ImageUploadField.tsx
│       │   ├── constants.ts
│       │   └── styles
│       └── Instructions.tsx
│           └── styles
│
├── constants.ts
├── types.ts
├── utils.ts
└── VerifiedIdUpload.module.css
```

## 🔄 Data Flow

```
┌─────────────────────────────────────────────────────────────────┐
│                        USER INTERACTION                          │
└────────────────────────────┬─────────────────────────────────────┘
                             │
                             ▼
┌─────────────────────────────────────────────────────────────────┐
│                   VerifiedIdUpload (Main)                       │
│                                                                 │
│  State:                                                         │
│  - editMode: boolean                                            │
│  - selectedImage: SelectedImage | null                          │
│  - submitting: boolean                                          │
│  - message: Message | null                                      │
└───────┬─────────────────────────────────────────┬───────────────┘
        │                                         │
        │                                         │
   ┌────▼──────┐                           ┌─────▼──────┐
   │  Hooks    │                           │ Components │
   └────┬──────┘                           └─────┬──────┘
        │                                        │
        │                                        │
┌───────▼────────────────────┐       ┌──────────▼──────────────┐
│   useExistingId()          │       │   View Mode / Edit Mode │
│                            │       │                         │
│   Returns:                 │       │   Receives:             │
│   - existingId             │──────▶│   - Props from main     │
│   - loading                │       │   - Callbacks           │
│   - error                  │       │   - State values        │
│   - refetch()              │       │                         │
└────────────────────────────┘       └─────────────────────────┘
                                                │
┌────────────────────────────┐                │
│   useIdForm(initType)      │                │
│                            │                │
│   Returns:                 │────────────────┘
│   - idType, files, step    │
│   - previews               │
│   - setters & handlers     │
│   - validation functions   │
└────────────────────────────┘
```

## 🎭 State Management

```
┌──────────────────────────────────────────────────────────────┐
│                    STATE OWNERSHIP                            │
└──────────────────────────────────────────────────────────────┘

┌─────────────────────┐
│  VerifiedIdUpload   │  Owns:
│  (Main Component)   │  - editMode
├─────────────────────┤  - selectedImage
│  Local State        │  - submitting
└─────────────────────┘  - message
         │
         ├──────────────────────────────────────┐
         │                                      │
    ┌────▼──────────┐                  ┌───────▼────────┐
    │ useExistingId │                  │   useIdForm    │
    │   (Hook)      │                  │    (Hook)      │
    ├───────────────┤                  ├────────────────┤
    │  Hook State   │                  │   Hook State   │
    │               │                  │                │
    │ - existingId  │                  │ - idType       │
    │ - loading     │                  │ - idFront      │
    │ - error       │                  │ - idBack       │
    └───────────────┘                  │ - selfie       │
                                       │ - step         │
                                       │ - previews     │
                                       └────────────────┘
```

## 🎯 Component Responsibilities

```
┌──────────────────────────────────────────────────────────────────┐
│  PRESENTATION LAYER (View Components)                            │
├──────────────────────────────────────────────────────────────────┤
│                                                                  │
│  IdViewMode                      IdEditMode                      │
│  ├── ImageViewCard (x3)         ├── StepIndicator               │
│  ├── MessageBanner              ├── IdTypeSelector              │
│  └── ImageModal                 ├── ImageUploadField (x3)       │
│                                 ├── MessageBanner               │
│                                 └── Instructions                │
└──────────────────────────────────────────────────────────────────┘

┌──────────────────────────────────────────────────────────────────┐
│  BUSINESS LOGIC LAYER (Hooks)                                    │
├──────────────────────────────────────────────────────────────────┤
│                                                                  │
│  useExistingId                   useIdForm                       │
│  ├── Fetch data                  ├── Form state                  │
│  ├── Loading state               ├── File handling              │
│  └── Refetch logic               ├── Preview generation         │
│                                  └── Validation                 │
└──────────────────────────────────────────────────────────────────┘

┌──────────────────────────────────────────────────────────────────┐
│  UTILITY LAYER (Pure Functions)                                  │
├──────────────────────────────────────────────────────────────────┤
│                                                                  │
│  utils.ts                       constants.ts        types.ts    │
│  ├── getImageUrl()              ├── ID_TYPES       Interfaces:  │
│  ├── formatUploadDate()         └── IMAGE_LABELS   ├── Message  │
│  └── createFilePreview()                           ├── IdForm   │
│                                                     └── Selected │
└──────────────────────────────────────────────────────────────────┘
```

## 🔀 User Flow Diagram

```
                        [User Visits Page]
                               │
                               ▼
                    ┌──────────────────────┐
                    │  useExistingId()     │
                    │  Fetches ID data     │
                    └──────────┬───────────┘
                               │
                    ┌──────────▼───────────┐
                    │  Has existing ID?    │
                    └──┬─────────────────┬─┘
                       │ NO              │ YES
                       │                 │
            ┌──────────▼─────┐    ┌─────▼──────────┐
            │  IdEditMode    │    │  IdViewMode    │
            │  (New Upload)  │    │  (Display ID)  │
            └────────┬───────┘    └────┬───────────┘
                     │                 │
        ┌────────────┼─────────────┐   │
        │            │             │   │
    [Step 1]    [Step 2]      [Submit] │
        │            │             │   │
        ▼            ▼             ▼   │
    ID Type      Selfie       API Call │
    Front/Back   Upload                │
                                       │
                                   [Edit] ◄───┐
                                       │      │
                                       ▼      │
                                 ┌──────────┐ │
                                 │IdEditMode│─┘
                                 │ (Edit)   │
                                 └────┬─────┘
                                      │
                                  [Submit]
                                      │
                                      ▼
                                [API Call]
                                      │
                                      ▼
                            ┌─────────────────┐
                            │ Success/Error   │
                            │ Message Display │
                            └─────────────────┘
```

## 🧩 Component Interaction

```
┌────────────────────────────────────────────────────────────────┐
│                    MAIN COMPONENT LOGIC                        │
└────────────────────────────────────────────────────────────────┘

  const { existingId, loading, refetch } = useExistingId()
                │           │         │
                │           │         └──── Called after submit
                │           └──────────── Shows loading state
                └──────────────────────── Determines view mode
                           │
                           ▼
         ┌─────────────────────────────┐
         │  Render Logic Decision      │
         └──────────┬──────────────────┘
                    │
         ┌──────────┴─────────┐
         │                    │
         ▼                    ▼
  [Has ID + !edit]      [Edit or !ID]
         │                    │
         ▼                    ▼
  ┌──────────────┐    ┌──────────────┐
  │ IdViewMode   │    │ IdEditMode   │
  └──────────────┘    └──────────────┘
         │                    │
         │                    │
    [Edit Click]         [Submit]
         │                    │
         └──────────┬─────────┘
                    │
                    ▼
            ┌──────────────┐
            │handleSubmit()│
            └──────┬───────┘
                   │
            ┌──────▼────────┐
            │  API Call     │
            └──────┬────────┘
                   │
            ┌──────▼────────┐
            │   refetch()   │
            └───────────────┘
```

## 🎨 Styling Architecture

```
VerifiedIdUpload.module.css
├── Container styles (.container)
├── Header styles (.header, .viewHeader)
├── Form styles (.form, .formGroup)
├── Step indicator (.stepIndicator, .stepItem)
├── Upload area (.uploadArea, .uploadLabel)
├── Preview styles (.previewContainer, .previewImage)
├── View mode (.viewGrid, .viewCard, .viewImage)
├── Modal styles (.modalOverlay, .modal)
├── Message styles (.message, .messageSuccess, .messageError)
├── Button group (.buttonGroup)
└── Instructions (.instructions, .instructionsList)

All components import this single CSS module
└── Maintains consistent styling across all components
```

## 🔒 Security Flow

```
┌────────────────────┐
│  User Uploads File │
└─────────┬──────────┘
          │
          ▼
┌─────────────────────────┐
│  createFilePreview()    │
│  (Client-side preview)  │
└─────────┬───────────────┘
          │
          ▼
┌─────────────────────────┐
│  FormData Creation      │
│  (File + metadata)      │
└─────────┬───────────────┘
          │
          ▼
┌─────────────────────────┐
│  POST /api/verified-id  │
│  (Server validates)     │
└─────────┬───────────────┘
          │
          ▼
┌─────────────────────────┐
│  File stored securely   │
│  (Server-side)          │
└─────────┬───────────────┘
          │
          ▼
┌─────────────────────────┐
│  View via API proxy     │
│  /api/verified-id/view  │
└─────────────────────────┘

No direct file system access from client
All images served through API proxy
User can only view their own files
```

## 📊 Performance Optimization Points

```
┌──────────────────────────────────────────────────────────────┐
│  OPTIMIZATION STRATEGIES                                      │
├──────────────────────────────────────────────────────────────┤
│                                                              │
│  1. Code Splitting                                           │
│     - IdViewMode only loads when needed                      │
│     - IdEditMode only loads when needed                      │
│                                                              │
│  2. Custom Hooks                                             │
│     - useExistingId fetches once, memoizes result           │
│     - useIdForm avoids prop drilling                         │
│                                                              │
│  3. Component Composition                                    │
│     - Small components = faster re-renders                   │
│     - Only affected components re-render                     │
│                                                              │
│  4. File Previews                                            │
│     - Generated client-side (no server roundtrip)           │
│     - Cached in state until form reset                       │
│                                                              │
│  5. Conditional Rendering                                    │
│     - Instructions only shown in new upload mode            │
│     - StepIndicator only shown when needed                  │
│                                                              │
└──────────────────────────────────────────────────────────────┘
```

## 🧪 Testing Strategy

```
┌──────────────────────────────────────────────────────────────┐
│  TESTING LAYERS                                               │
├──────────────────────────────────────────────────────────────┤
│                                                              │
│  Unit Tests                                                  │
│  ├── utils.ts functions                                      │
│  │   ├── getImageUrl()                                       │
│  │   ├── formatUploadDate()                                  │
│  │   └── createFilePreview()                                 │
│  │                                                           │
│  ├── useIdForm hook                                          │
│  │   ├── State updates                                       │
│  │   ├── Validation logic                                    │
│  │   └── File handling                                       │
│  │                                                           │
│  └── useExistingId hook                                      │
│      ├── API calls (mocked)                                  │
│      └── Loading states                                      │
│                                                              │
├──────────────────────────────────────────────────────────────┤
│                                                              │
│  Component Tests                                             │
│  ├── MessageBanner                                           │
│  ├── StepIndicator                                           │
│  ├── IdTypeSelector                                          │
│  ├── ImageUploadField                                        │
│  ├── ImageViewCard                                           │
│  └── ImageModal                                              │
│                                                              │
├──────────────────────────────────────────────────────────────┤
│                                                              │
│  Integration Tests                                           │
│  ├── IdViewMode                                              │
│  ├── IdEditMode                                              │
│  └── VerifiedIdUpload (full flow)                           │
│                                                              │
└──────────────────────────────────────────────────────────────┘
```

## 🔄 Future Extension Points

```
┌──────────────────────────────────────────────────────────────┐
│  WHERE TO ADD NEW FEATURES                                    │
├──────────────────────────────────────────────────────────────┤
│                                                              │
│  New ID Type                                                 │
│  └── constants.ts → Add to ID_TYPES array                   │
│                                                              │
│  New Image Type                                              │
│  ├── constants.ts → Add to ImageType & IMAGE_LABELS         │
│  ├── types.ts → Extend IdFormState                          │
│  └── useIdForm.ts → Add state handling                      │
│                                                              │
│  Image Compression                                           │
│  └── useIdForm.ts → handleFileChange() → compress before    │
│                      creating preview                        │
│                                                              │
│  Drag-and-Drop                                               │
│  └── ImageUploadField.tsx → Add onDrop handler              │
│                                                              │
│  Camera Capture                                              │
│  └── Create new CameraCapture.tsx component                 │
│      └── Import in ImageUploadField or IdEditMode           │
│                                                              │
│  Progress Indicators                                         │
│  ├── Create UploadProgress.tsx component                    │
│  └── Update handleSubmit in VerifiedIdUpload.tsx           │
│                                                              │
│  Additional Validation                                       │
│  └── useIdForm.ts → Add validation functions                │
│                                                              │
└──────────────────────────────────────────────────────────────┘
```

---

This architecture provides clear separation of concerns, making the codebase maintainable, testable, and scalable.